section.slide
  h3
    | Переменные
  .columns.code
    .col
      h3
        | SCSS
      |<pre class="hightlight"><code>
      |<span>$font-stack:</span> <span>Helvetica, sans-serif;</span>
      |<span>$primary-color:</span> #333;
      |
      |<span>body</span>{
      |<span class="key">font:</span> $font-stack;
      |<span class="key">background-color:</span> $primary-color;
      |}
      |</code></pre>
    .col
      h3
        | CSS
      |<pre class="hightlight"><code>
      |
      |<span>body</span> {
      |<span class="key">font:</span> 100% Helvetica, sans-serif;
      |<span class="key">background-color:</span> #efefef;
      |}
      |</code></pre>
